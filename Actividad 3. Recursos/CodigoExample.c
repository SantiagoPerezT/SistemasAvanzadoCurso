#include <stdio.h>
int main(){
    int entero = 10;
    float flotante = 5.5;
    float resultado = entero + flotante; // Conversión implícita de int a float
    printf("Resultado: %f\n", resultado);
    return 0;
}

